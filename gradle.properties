# Root gradle.properties — общие настройки, per-version переопределяется в versions/<ver>/gradle.properties
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Общее (одинаково во всех версиях)
mod_version=1.0.0
maven_group=com.paketrool.casinocraft
loader_version=0.19.3
loom_version=1.10-SNAPSHOT

# Дефолт (перебиваются per-version)
fabric_api_version=0.128.2+1.21.5
